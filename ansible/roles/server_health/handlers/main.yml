---
- name: reload systemd
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart server-health-responder
  become: true
  ansible.builtin.systemd:
    name: server-health-responder.service
    state: restarted
