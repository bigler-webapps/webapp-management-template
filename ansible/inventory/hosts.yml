---
# Inventory — REPLACE after "Use this template"
#
# Each host name must match a corresponding `host_vars/<name>.yml` file
# (see host_vars/example-prod.yml.example for the schema).
# Each host name should also match a GitHub Environment of the same name
# in this webapp-management instance.
all:
  children:
    stage:
      hosts: {}
        # Example: staging: {}
    prod:
      hosts: {}
        # Example: main-prod: {}
        # Example: contact-prod: {}
