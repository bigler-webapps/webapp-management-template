---
collections:
  - name: biglerconsult.infra
    source: git+https://github.com/bigler-webapps/workflow-templates.git#/ansible
    type: git
    # Bumped to pre-bake the Claude Code CLI (@anthropic-ai/claude-code) into the
    # github_runner role's runner image (only that role changed in this bump).
    version: f91f71ad4eb526704209dfe1352afbbd84ec53df
